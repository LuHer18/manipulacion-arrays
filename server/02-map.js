const letters = ["a", "b", "c"];

const newArray = letters.map(item => item + 'b');
console.log(newArray);

/* for (let index = 0; index < letters.length; index++) {
    const element = letters[index];

    newArray.push(element +"b");
    
}
console.log(newArray); */
